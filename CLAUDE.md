# Claude Project Instructions

Follow the shared repository agent instructions in `AGENTS.md`.

In particular, before starting work, inspect `docs/agent-skills/` and use any matching skill. The shared skills are platform-neutral; `.codex/skills/` contains Codex adapters only.
